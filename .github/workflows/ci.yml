name: Frontend CI Pipeline
run-name: Frontend CI Pipeline

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  lint:
    runs-on: self-hosted
    steps:
      - run: echo "npx eslint ."

#   test:
#     needs: lint
#     runs-on: self-hosted
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#       - run: npm ci
#       - run: npm test
#
#   sast:
#     needs: lint
#     runs-on: self-hosted
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#       - run: make build coverage sonar-scan

  ci:
    #needs: [test, sast]
    uses: raghudevopsb89/github-actions/.github/workflows/ci.yml@main
    with:
      component_name: frontend

